<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.learn.mlanalysissysbackend.mapper.AdminMapper">
    <insert id="addAdmin" useGeneratedKeys="true" keyProperty="id">
        insert into admin(name, password, create_time, update_time)
        values (#{name}, #{password}, #{createTime}, #{updateTime})
    </insert>

    <select id="getAdminByName" resultType="com.learn.mlanalysissysbackend.pojo.Admin">
        select *
        from admin
        where name = #{name}
    </select>


</mapper>